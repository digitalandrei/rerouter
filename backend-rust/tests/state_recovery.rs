//! Skeleton: on startup, non-terminal reroutes become `uncertain` and lock the
//! asset. See ../docs/state-recovery.md. Fill in with a test DB container.

// #[tokio::test] async fn pending_actions_become_uncertain() { ... }
