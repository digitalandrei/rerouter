/**
 * /providers — placeholder page (reroute providers are not in the SNMP-first
 * API contract; this page is retained for future use).
 */
export default function Providers() {
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold tracking-tight">Reroute providers</h1>
      <p className="text-sm text-muted-foreground">
        Reroute provider management is not yet available in this build.
      </p>
    </div>
  );
}
